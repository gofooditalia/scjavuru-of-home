---
import { t } from '../i18n/index';
import type { Lang } from '../i18n/index';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const tr = t(lang);
const homeHref = lang === 'it' ? '/' : '/en/';
---

<footer class="bg-slate-900 text-slate-300 py-12">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-10 mb-10">

      <!-- Brand -->
      <div>
        <a href={homeHref} class="font-display font-bold text-xl text-white hover:text-sky-400 transition-colors">
          Mondello Holiday
        </a>
        <p class="text-slate-400 mt-2 text-sm leading-relaxed">{tr.footer.tagline}</p>
      </div>

      <!-- Navigation -->
      <div>
        <h3 class="text-xs font-semibold tracking-widest uppercase text-slate-500 mb-4">Menu</h3>
        <nav class="flex flex-col gap-2">
          {[
            { href: '#galleria', label: tr.nav.gallery },
            { href: '#prenota', label: tr.nav.booking },
            { href: '#dove-siamo', label: tr.nav.location },
            { href: '#prezzi', label: tr.nav.prices },
          ].map(({ href, label }) => (
            <a href={href} class="text-sm text-slate-400 hover:text-white transition-colors">{label}</a>
          ))}
        </nav>
      </div>

      <!-- Contact -->
      <div>
        <h3 class="text-xs font-semibold tracking-widest uppercase text-slate-500 mb-4">{tr.footer.contact}</h3>
        <a
          href="mailto:italiagofood@gmail.com"
          class="flex items-center gap-2 text-sm text-slate-400 hover:text-white transition-colors"
        >
          <svg class="w-4 h-4 text-sky-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
          </svg>
          italiagofood@gmail.com
        </a>
      </div>
    </div>

    <!-- Bottom bar -->
    <div class="border-t border-slate-800 pt-6 flex flex-col sm:flex-row items-center justify-between gap-3 text-xs text-slate-600">
      <p>{tr.footer.rights}</p>
      <div class="flex gap-4">
        <a href="/" class={['hover:text-slate-400 transition-colors', lang === 'it' ? 'text-slate-400' : ''].join(' ')}>IT</a>
        <span>|</span>
        <a href="/en/" class={['hover:text-slate-400 transition-colors', lang === 'en' ? 'text-slate-400' : ''].join(' ')}>EN</a>
      </div>
    </div>
  </div>
</footer>
